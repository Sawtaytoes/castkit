{
  "extends": "@charcuterie/tsconfig/base",
  "compilerOptions": {
    // The fleet invariants live in @charcuterie/tsconfig/base; only
    // CastKit's deltas stay here.
    "allowJs": false,
    "allowImportingTsExtensions": true,
  }
}
